{% import 'macros.mona' as macros %}

{% call macros.pred_def("deadlock", formula.bound_to.states, []) -%}
{% call(clause) macros.blockify("&", formula.clauses) %}
all1 {{ macros.join(clause.existential_terms|map("render_term_filter")) }}: (
  (
    {{ macros.clause_term_conditions(clause) }}
  ) => (
    {{ macros.pred_call("dead_transition_" ~ clause.index, formula.bound_to.states, clause.existential_terms) }}
  )
)
{%- endcall %}
{%- endcall %}
